tools:
    external_code_coverage:
        timeout: 300
build:
    dependencies:
        before:
            - sudo apt-get update
            - sudo apt-get install -y openssl libssl-dev
        nodes:
            analysis:
                tests:
                    override:
                        - php-scrutinizer-run
