tools:
    external_code_coverage:
        timeout: 300
build:
    environment:
    php:
      version: 8.1 # Specify your PHP version
      pecl_extensions:
        - gd    
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run
